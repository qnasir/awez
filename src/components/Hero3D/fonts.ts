/**
 * Self-hosted, subset to the ~90 glyphs the scene actually draws.
 * The full families are 650 KB; these are 40 KB and on-brand, where troika's
 * default would silently fall back to Roboto.
 */
export const FONT_DISPLAY = '/fonts/archivo-800.ttf'
export const FONT_UI = '/fonts/inter-500.ttf'
